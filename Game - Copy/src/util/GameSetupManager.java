package util;

import java.awt.*;
import java.io.IOException;

public class GameSetupManager {
    public static void SetupImages(Color color) throws IOException {
        //for(int i = 0; i < 16; i++)
        //    ImageUtils.SaveImage("/sprite/player/edited/player_" + String.format("%02d", i) + ".png", ImageUtils.SwapPlayerSprite(ImageUtils.ReadImage("/sprite/player/original/player_" + i + ".png")));
    }
}
